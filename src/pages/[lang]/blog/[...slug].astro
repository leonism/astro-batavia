---
// This file is deprecated in favor of [...page].astro which handles both pagination and posts to avoid route conflicts.
// We keep this file to satisfy the "do not delete" constraint, but it generates no paths.

export async function getStaticPaths() {
  return [];
}
---
