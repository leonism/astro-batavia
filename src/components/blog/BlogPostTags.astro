---
import { getLocalizedPath, slugifyTag } from '@/i18n/utils';

interface Props {
  tags: string[];
  lang: string;
}

const { tags, lang } = Astro.props;
---

{
  tags && tags.length > 0 && (
    <div class="mt-6" role="group" aria-label="Article tags">
      <div class="flex flex-wrap gap-2">
        {tags.map((tag: string) => (
          <a
            href={getLocalizedPath(`/blog/tags/${slugifyTag(tag)}`, lang)}
            class="inline-flex items-center rounded-full bg-primary-100 px-3 py-1 text-xs font-medium text-primary-800 transition-colors duration-200 hover:bg-primary-200 dark:bg-primary-900 dark:text-primary-200 dark:hover:bg-primary-800"
          >
            #{tag}
          </a>
        ))}
      </div>
    </div>
  )
}
