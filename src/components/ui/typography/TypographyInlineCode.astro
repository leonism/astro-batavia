---
interface Props {
  class?: string;
  [x: string]: any;
}
const { class: className = '', ...rest } = Astro.props;
---
<code class:list={["relative rounded bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm font-semibold", className]} {...rest}>
  <slot />
</code>
