---
interface Props {
  class?: string;
  [x: string]: any;
}
const { class: className = '', ...rest } = Astro.props;
---

<p class:list={['text-sm font-stretch-semi-expanded text-muted-foreground', className]} {...rest}>
  <slot />
</p>
