---
interface Props {
  class?: string;
  [x: string]: any;
}
const { class: className = '', ...rest } = Astro.props;
---
<h2 class:list={["scroll-m-20 border-b pb-2 text-3xl font-semibold tracking-tight first:mt-0", className]} {...rest}>
  <slot />
</h2>
