---
interface Props {
  class?: string;
  [x: string]: any;
}
const { class: className = '', ...rest } = Astro.props;
---
<div class:list={["my-6 w-full overflow-y-auto", className]} {...rest}>
  <table class="w-full">
    <slot />
  </table>
</div>
