---
interface Props {
  class?: string;
  [x: string]: any;
}
const { class: className = '', ...rest } = Astro.props;
---
<div class:list={["text-lg font-semibold", className]} {...rest}>
  <slot />
</div>
